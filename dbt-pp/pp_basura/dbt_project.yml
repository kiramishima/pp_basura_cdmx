name: 'pp_basura'
version: '1.0.0'

profile: 'pp_basura'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"

models:
  pp_basura:
    stagging:
      +materialized: view
    core:
      materialized: table
